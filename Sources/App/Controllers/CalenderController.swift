//
//  CalenderController.swift
//  App
//
//  Created by Johan Thorell on 2019-10-20.
//

import Vapor
import FluentPostgreSQL

//typealias ClosedDateRange = (startDate: Date, endDate: Date)

//struct CalenderController {
//	
//	let events: [ClosedDateRange]
//	func getAvailableSpots(using timeslot: Timeslot) -> [ClosedDateRange] {
//		for i in 0..<events.count {
////			let currentEvent = events[i]
////			let nextEvent = events[i + 1]
////			let time = currentEvent.endDate.timeIntervalSince(nextEvent.startDate)
////			if time
//		}
//	}
//}
